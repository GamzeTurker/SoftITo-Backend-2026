@model IEnumerable<OkulNotSistemi.Models.Ogrenciler>

@{
    ViewData["Title"] = "Öğrenciler";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h3>👨‍🎓 Öğrenciler</h3>

    <a href="/Ogrenciler/Create" class="btn btn-primary">➕ Yeni Öğrenci</a>
</div>

<!-- 🔍 ARAMA -->
<input type="text" id="searchInput" class="form-control mb-3" placeholder="Öğrenci ara...">

<div class="card shadow">
    <div class="card-body">

        <div class="table-responsive">
            <table class="table table-bordered table-hover" id="table">

                <thead class="thead-dark">
                    <tr>
                        <th>Ad</th>
                        <th>Soyad</th>
                        <th>Numara</th>
                        <th>Sınıf</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>

                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.OgrenciAd</td>
                            <td>@item.OgrenciSoyad</td>
                            <td>@item.Numara</td>
                            <td>@item.Sinif</td>

                            <td>
                                <a href="/Ogrenciler/Edit/@item.OgrenciId" class="btn btn-warning btn-sm">Edit</a>
                                <a href="/Ogrenciler/Delete/@item.OgrenciId" class="btn btn-danger btn-sm">Delete</a>
                            </td>
                        </tr>
                    }
                </tbody>

            </table>
        </div>

    </div>
</div>

<!-- 🔍 SEARCH SCRIPT -->
<script>
    document.getElementById("searchInput").addEventListener("keyup", function () {
        var value = this.value.toLowerCase();
        var rows = document.querySelectorAll("#table tbody tr");

        rows.forEach(row => {
            row.style.display = row.innerText.toLowerCase().includes(value)
                ? ""
                : "none";
        });
    });
</script>